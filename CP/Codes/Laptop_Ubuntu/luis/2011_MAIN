#include<bits/stdc++.h>
using namespace std;

int main(){
	string a , b;
	while( cin >> a >> b ){
		int na = a.size();
		int nb = b.size();
		REP( i , na ) a[ i ] -= 'a';
		REP( i , nb ) b[ i ] -= 'a';
	}
}
